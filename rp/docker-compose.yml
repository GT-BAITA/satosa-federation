services:
  satosa-rp:
    build: .
    ports:
      - "127.0.0.1:9003:8080"
    volumes:
      - ./etc:/opt/satosa/etc
      - ./plugin:/opt/satosa/plugin
    environment:
      SATOSA_STATE_ENCRYPTION_KEY: "${SATOSA_STATE_ENCRYPTION_KEY}"
      SATOSA_CONFIG: "/opt/satosa/etc/proxy_conf.yaml"
    restart: unless-stopped
